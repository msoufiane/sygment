import React from 'react';
import { Header, Menu } from '../ui';

function Routes() {
    return (
        <React.Fragment>
            <Header />
            <Menu />
        </React.Fragment>
    );
}

export default Routes;
